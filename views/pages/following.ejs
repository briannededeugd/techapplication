<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <%- include('../includes/head'); %>

    <title>Following</title>
</head>

<body>
    <header>
        <%- include('../includes/header'); %>
    </header>

    <main class='following-main'>
        <h1>Following</h1>
        <p>Here you can manage the people you're following</p>
        <div id="empty-message" class="<%= EMPTY_MESSAGE_H2 ? '' : 'empty-message-hidden' %>">
            <h2><%=EMPTY_MESSAGE_H2 %></h2>
            <img src="<%= EMPTY_IMAGE %>">
            <p><%= EMPTY_MESSAGE_P %></p>
        </div>
        <div id='following-profiles'>
            <% FOLLOWING_ARRAY.forEach((profile) => { %>
              <div class="<%= profile.subId %>">
                <h2><%= profile.name %></h2>
                <p>Age:<%= profile.age %></p>
                <p><%= profile.songs %></p>
                <p><%= profile.about %></p>
                <form action="/following/<%= profile.subId %>" method="POST">
                  <!-- <input type="hidden" name="profile_id" value="<%= profile.subId %>"> -->
                  <input type="hidden" name="followStatus" value="<%= profile.follow ? 'false' : 'true' %>">
                  <% if (profile.follow) { %>
                    <button>Unfollow</button>
                  <% } else { %>
                    <button>Follow</button>
                  <% } %>
                </form>
                <img src='<%= profile.image %>'>
              </div>
            <% }); %>
          </div>

    
    </main>

    <footer>
        <%- include('../includes/footer'); %>
    </footer>
    
</body>
</html>







