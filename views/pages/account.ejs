<%- include("../partials/head") %>

<body>
    <header>
        <%- include("../partials/header") %>
    </header>
    <main>
        <h1>Hello</h1>
        <p><%= user.firstName + ' ' + user.lastName%></p>
        <ul>
            <% user.mood.forEach(moods => { %>
                <li><%= moods %></li>
            <% }); %>
        </ul>
    </main>
</body>
</html>